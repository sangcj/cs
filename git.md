```python

git colne 网络仓库地址

git status 查看当前分支

git add <files> 提交文件到缓存区

git config --global user.email "1607470930@qq.com" 设置提交全局用户

git commit -m 'read' 提交缓存区文件到本地仓库

git push -u origin master 提交到远程仓库

git branch <branch-name> 创建分支

git checkout <branch-name> 切换分支

```


